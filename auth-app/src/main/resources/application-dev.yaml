#developments configurations
server:
  port: 8081

#database connections
#JPA and Hibernate configurations
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_app
    username: root
    password: Prince@0717
#    hikari:
#      pool-name: Hikari
#      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true