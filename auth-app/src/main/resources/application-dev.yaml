#developments configurations
server:
  port: 8081

#database connections
#JPA and Hibernate configurations
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_app
    username: root
    password: Prince@0717
#    hikari:
#      pool-name: Hikari
#      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true

Security:
  jwt:
    secret: ${JWT SECRET:9f8a7c6b5d4e3f2a1b0c9d8e7f6a5b4c3d2e1f0a9b8c7d6e5f4a3b2c1d0e9f8a}
    issuer: ${JWT ISSUER:api.prince.com}
    access-ttl-seconds: ${JWT ACCESS TTL SECONDS:3600}
    refresh-ttl-seconds: ${JWT REFRESH TTL SECONDS:86400}
    refresh-token-cookie-name: ${JWT REFRESH COOKIE NAME:refreshToken}
    cookie-secure: ${JWT COOKIE SECURE:true}
    cookie-http-only: ${JWT COOKIE HTTP ONLY:true}
    Cookie-same-site: ${JWT COOKIE SAME SITE:lax}
